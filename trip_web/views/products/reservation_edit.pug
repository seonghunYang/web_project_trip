extends ../layout

block content
  .container
    h1.main-title 예약 수정
    form(action=`/products/${reservation.id}?_method=PUT`, method="POST")
      include _form
      button.btn.btn-outline-primary(type="submit") 수정완료